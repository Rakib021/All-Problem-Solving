#include <stdio.h>
int main()
{
    int t, runs[10], i;
    scanf("%d", &t);

    for (i = 1; i <= 6; i++)
    {
        scanf("%d", &runs[i]);
        if (runs[i] != 0)
        {
            break;
        }
        else{

        }
    }
    printf("YES");
}